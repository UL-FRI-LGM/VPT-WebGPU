export namespace MIXINS {
    const Photon: string;
    const XYZITU2002: string;
    const YUVBT601: string;
    const YUVBT709: string;
    const btrand: string;
    const constants: string;
    const encodeFloat: string;
    const floatToRgba: string;
    const hue: string;
    const intersectCube: string;
    const quasiCubicSampling: string;
    const rand: string;
    namespace random {
        namespace distribution {
            const ball: string;
            const circle: string;
            const disk: string;
            const exponential: string;
            const hemisphere: string;
            const normal: string;
            const sphere: string;
            const square: string;
            const uniformcast: string;
            const uniformdivision: string;
        }
        namespace hash {
            const bbs: string;
            const jenkins: string;
            const lcg: string;
            const pcg: string;
            const squashlinear: string;
            const squashnested: string;
            const squashxor: string;
            const wang: string;
            const xorshift: string;
            const xxhash: string;
        }
    }
    const rgbaToFloat: string;
    const unproject: string;
    const unprojectRand: string;
}
